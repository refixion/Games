import { useEffect, useState } from 'react'
import { API_BASE_URL, GamePreset } from '../config'
export default function Game() { const [game, setGame] = useState<GamePreset | null>(null); useEffect(() => { fetch(`${API_BASE_URL}/game-state`).then((r) => r.json()).then((d) => setGame(d.game)) }, []); if (!game) return <p className="text-slate-400">Uitleg laden…</p>; return <div className="space-y-8"><header className="border-b border-white/10 pb-8"><p className="text-xs uppercase tracking-[0.25em] text-amber-400">Speluitleg / {game.type}</p><h1 className="mt-3 text-5xl font-black text-white">{game.name}</h1><p className="mt-4 max-w-2xl text-lg text-slate-300">{game.description}</p></header><section className="grid gap-6 md:grid-cols-2"><div><h2 className="section-title">Doel</h2><p className="text-slate-300">{game.goal}</p><h2 className="section-title">Spelregels</h2><ul className="list-disc space-y-2 pl-5 text-slate-300">{game.rules.map((rule) => <li key={rule}>{rule}</li>)}</ul></div><div><h2 className="section-title">Verloop</h2><ol className="space-y-3">{game.steps.map((step, i) => <li key={step} className="flex gap-3 text-slate-300"><b className="text-amber-300">0{i + 1}</b>{step}</li>)}</ol><h2 className="section-title">Rollen</h2><div className="space-y-3">{game.roles.map((role) => <div key={role.name} className="border border-white/10 p-4"><b className="text-white">{role.name}</b><p className="mt-1 text-sm text-slate-400">{role.description}</p></div>)}</div></div></section></div> }
